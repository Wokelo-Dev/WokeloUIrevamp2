import { Client } from '@hyper-fetch/core';

export const client = new Client({
  url: "https://wkprod.eastus.cloudapp.azure.com/api",
  // url: "https://wokelo-dev.eastus.cloudapp.azure.com/api",
});
